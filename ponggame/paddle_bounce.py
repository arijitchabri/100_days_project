# from ball import Ball
# from paddle import Paddle
#
# class PaddleBounce(Ball, Paddle):
#     def __init__(self):
#         super.__init__(self)
#
    # def paddle_collision(self):
    #     b = self.ball
    #     p = self.paddle
    #     if b.distance(p.xcor(), p.ycor()) <= 10:
    #         self.bounce_x()
    #     if b.xcor >= 360 or b.xcor() <= 360:
    #         if b.distance(p.xcor(), p.ycor()) <= 30:
    #             self.bounce_x()
